#include <iostream>
#include <stdio.h>

#define MAX 1002

using namespace std;

int N;
int P[MAX] = {0,};

void Solution()
{
    for(int i=1; i<=N; i++)
    {
        
    }
}

int main()
{
    ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
    cin >> N;

    for(int i=1; i<=N; i++)
    {
        cin >> P[i];
    }

    Solution();

    return 0;
}

// 앞에서 부터 반복을 하면서 만약에 